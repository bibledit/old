#!/bin/bash

g++ `pkg-config sqlite3 --cflags --libs` `pkg-config icu-i18n --cflags --libs` `xml2-config --cflags` `xml2-config --libs` -std=c++11 -o morphhb morphhb.cpp

if [ $? -ne 0 ]; then
  exit
fi

./morphhb
