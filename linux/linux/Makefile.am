bin_PROGRAMS = bibledit

bibledit_SOURCES = bibledit.cpp

AM_CXXFLAGS = -Wall -Wextra -pedantic -std=c++11 $(GTK_CFLAGS)

LIBS = -Wall $(GTK_LIBS)

EXTRA_DIST = *.h *.desktop *.xpm

CLEANFILES = bibledit*.gz *~

man_MANS = *.1
